-- tests/test_unique_date_granularity.sql

SELECT
    date

FROM {{ ref('mart_daily_kpis') }}

-- Group by date and find any date that appears more than once
GROUP BY date
HAVING COUNT(*) > date